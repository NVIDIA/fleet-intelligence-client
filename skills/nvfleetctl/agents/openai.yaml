interface:
  display_name: "NVIDIA Fleet Intelligence CLI"
  short_description: "Query GPU fleet health with nvfleetctl"
  default_prompt: "Use $nvfleetctl to answer a question about my NVIDIA Fleet Intelligence fleet."
